version: '3.8'

services:
  backend:
    build: .
    container_name: movie-app-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./backend:/app/backend
    environment:
      - DATABASE_URL=sqlite:///./userdb.db
    restart: unless-stopped
